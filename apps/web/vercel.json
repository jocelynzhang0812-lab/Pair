{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @pair/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
