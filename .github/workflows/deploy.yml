name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Web
        run: pnpm --filter @pair/web build
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}

      # 取消下面注释并配置 VERCEL_TOKEN / VERCEL_ORG_ID / VERCEL_PROJECT_ID 即可自动部署到 Vercel
      # - name: Deploy to Vercel
      #   uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     working-directory: ./apps/web

  deploy-api:
    runs-on: ubuntu-latest
    needs: deploy-web
    steps:
      - uses: actions/checkout@v4

      # Render 自动部署：在 Render Dashboard 里配置 Auto-Deploy from GitHub 即可，无需额外 workflow
      # Fly.io 取消下面注释并配置 FLY_API_TOKEN
      # - uses: superfly/flyctl-actions/setup-flyctl@master
      # - run: flyctl deploy --dockerfile apps/api/Dockerfile --remote-only
      #   env:
      #     FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      # 阿里云 ECS / 自建服务器取消下面注释并配置 SSH 密钥
      # - name: Deploy to ECS
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.ECS_HOST }}
      #     username: ${{ secrets.ECS_USER }}
      #     key: ${{ secrets.ECS_SSH_KEY }}
      #     script: |
      #       cd /opt/pair
      #       git pull origin main
      #       docker compose -f apps/api/docker-compose.prod.yml up -d --build
